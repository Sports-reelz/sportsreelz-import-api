fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
python-dotenv>=1.0.0
requests>=2.31.0
yt-dlp>=2024.1.0
boto3>=1.34.0
playwright>=1.40.0
# yt-dlp plugin: generates Proof-of-Origin Tokens via a sidecar HTTP server,
# which is the only reliable way past YouTube's 2026 datacenter-IP bot gate.
# Inert when no BGUTIL_BASE_URL is configured, so safe to keep installed
# whether or not the bgutil sidecar is actually running.
bgutil-ytdlp-pot-provider>=0.5.0
